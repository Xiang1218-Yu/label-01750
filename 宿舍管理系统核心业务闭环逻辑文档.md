# 宿舍管理系统核心业务闭环逻辑文档

## 一、系统概述

本宿舍管理系统基于 Spring Boot 3 + Vue 3 技术栈构建，实现了完整的宿舍管理业务闭环，支持**管理员**、**宿管**、**学生**三种角色，通过 RBAC 权限控制体系实现数据隔离与业务流转。

---

## 二、学生入住-退宿完整业务流程

### 2.1 入住分配床位流程

**业务流程图：**
```
1. 管理员/宿管新增学生信息
   ↓
2. 系统自动创建学生账号（用户名=学号，默认密码=123456）
   ↓
3. 管理员/宿管为学生分配空闲床位
   ↓
4. 系统校验：学生无床位、床位未被占用
   ↓
5. 绑定床位：更新床位状态为"已占用"，关联学生ID
   ↓
6. 更新房间当前入住人数+1
   ↓
7. 分配完成，学生可查看宿舍信息
```

**核心代码逻辑**（`StudentService.java:87-111`）：

```java
@Transactional
public void bindBed(Long studentId, Long bedId) {
    // 1. 校验学生存在
    Student student = studentMapper.selectById(studentId);
    // 2. 校验学生无床位
    Bed existingBed = bedMapper.selectOne(
        new LambdaQueryWrapper<Bed>().eq(Bed::getStudentId, studentId));
    // 3. 校验床位存在且空闲
    Bed bed = bedMapper.selectById(bedId);
    // 4. 绑定床位
    bed.setStudentId(studentId);
    bed.setStatus(1);  // 1=已占用
    bedMapper.updateById(bed);
    // 5. 更新房间人数
    Room room = roomMapper.selectById(bed.getRoomId());
    room.setCurrentCount(room.getCurrentCount() + 1);
    roomMapper.updateById(room);
}
```

### 2.2 退宿释放床位流程

**业务流程图：**
```
1. 管理员/宿管发起退宿操作
   ↓
2. 系统查询学生当前绑定的床位
   ↓
3. 解除床位绑定：清空学生ID，床位状态置为"空闲"
   ↓
4. 更新房间当前入住人数-1
   ↓
5. 退宿完成，床位可重新分配
```

**核心代码逻辑**（`StudentService.java:113-128`）：

```java
@Transactional
public void unbindBed(Long studentId) {
    // 1. 查询学生当前床位
    Bed bed = bedMapper.selectOne(
        new LambdaQueryWrapper<Bed>().eq(Bed::getStudentId, studentId));
    // 2. 解除绑定（使用UpdateWrapper处理null值更新）
    bedMapper.update(null, new LambdaUpdateWrapper<Bed>()
            .eq(Bed::getId, bed.getId())
            .set(Bed::getStudentId, null)
            .set(Bed::getStatus, 0));  // 0=空闲
    // 3. 更新房间人数
    Room room = roomMapper.selectById(bed.getRoomId());
    room.setCurrentCount(Math.max(0, room.getCurrentCount() - 1));
    roomMapper.updateById(room);
}
```

### 2.3 房间-床位数据模型

**床位状态机：**
| 状态值 | 状态说明 |
|--------|----------|
| 0 | 空闲（可分配） |
| 1 | 已占用 |

**数据关联关系：**
```
楼栋 (building)
   ↓ 1:N
房间 (room) - 包含容量、当前人数
   ↓ 1:N
床位 (bed) - 关联学生ID、状态标记
   ↓ 1:1
学生 (student) - 关联用户账号
   ↓ 1:1
系统用户 (sys_user) - 登录账号、角色
```

---

## 三、角色权限隔离体系

系统采用**基于角色的访问控制（RBAC）**，通过 `@RequireRole` 注解实现接口级权限控制。

### 3.1 角色定义

| 角色ID | 角色名称 | 说明 |
|--------|----------|------|
| 1 | 管理员 | 系统最高权限，可管理所有模块 |
| 2 | 宿管 | 楼栋管理人员，负责日常运营 |
| 3 | 学生 | 普通用户，仅可操作个人相关功能 |

### 3.2 权限矩阵

#### 3.2.1 楼栋管理模块

| 操作 | 管理员 | 宿管 | 学生 | 接口位置 |
|------|--------|------|------|----------|
| 新增楼栋 | ✓ | ✗ | ✗ | `BuildingController.java:37-42` |
| 修改楼栋 | ✓ | ✗ | ✗ | `BuildingController.java:44-50` |
| 删除楼栋 | ✓ | ✗ | ✗ | `BuildingController.java:52-57` |
| 查看楼栋列表 | ✓ | ✓ | ✗ | `BuildingController.java:31-35` |
| 分页查询楼栋 | ✓ | ✗ | ✗ | `BuildingController.java:23-29` |

**权限控制代码**（`BuildingController.java:18`）：
```java
@RequireRole({RoleConstants.ADMIN})  // 仅管理员可访问
public class BuildingController { ... }
```

#### 3.2.2 房间/床位管理模块

| 操作 | 管理员 | 宿管 | 学生 | 接口位置 |
|------|--------|------|------|----------|
| 新增房间 | ✓ | ✓ | ✗ | `RoomController.java:35-40` |
| 修改房间 | ✓ | ✓ | ✗ | `RoomController.java:42-48` |
| 删除房间 | ✓ | ✓ | ✗ | `RoomController.java:50-55` |
| 查看房间详情 | ✓ | ✓ | ✗ | `RoomController.java:30-33` |
| 查看床位列表 | ✓ | ✓ | ✗ | `RoomController.java:57-60` |

**权限控制代码**（`RoomController.java:17`）：
```java
@RequireRole({RoleConstants.ADMIN, RoleConstants.DORM_MANAGER})
public class RoomController { ... }
```

#### 3.2.3 学生管理模块

| 操作 | 管理员 | 宿管 | 学生 | 接口位置 |
|------|--------|------|------|----------|
| 新增学生 | ✓ | ✓ | ✗ | `StudentController.java:30-35` |
| 修改学生 | ✓ | ✓ | ✗ | `StudentController.java:37-43` |
| 删除学生 | ✓ | ✓ | ✗ | `StudentController.java:45-50` |
| 分配床位 | ✓ | ✓ | ✗ | `StudentController.java:52-57` |
| 退宿操作 | ✓ | ✓ | ✗ | `StudentController.java:59-64` |
| 分页查询学生 | ✓ | ✓ | ✗ | `StudentController.java:22-28` |

**权限控制代码**（`StudentController.java:17`）：
```java
@RequireRole({RoleConstants.ADMIN, RoleConstants.DORM_MANAGER})
public class StudentController { ... }
```

#### 3.2.4 维修申请模块

| 操作 | 管理员 | 宿管 | 学生 | 接口位置 |
|------|--------|------|------|----------|
| 查看所有维修申请 | ✓ | ✓ | ✗ | `RepairController.java:21-27` |
| 查看我的维修申请 | ✓ | ✓ | ✓ | `RepairController.java:29-34` |
| 提交维修申请 | ✓ | ✓ | ✓ | `RepairController.java:36-42` |
| 处理维修申请 | ✓ | ✓ | ✗ | `RepairController.java:44-52` |

**业务逻辑说明**：
- 学生提交时：系统自动获取当前登录学生的宿舍信息（`RepairService.java:39-60`）
- 管理员/宿管可代为提交：需手动指定学生和房间

#### 3.2.5 访客登记模块

| 操作 | 管理员 | 宿管 | 学生 | 接口位置 |
|------|--------|------|------|----------|
| 查看所有访客记录 | ✓ | ✓ | ✗ | `VisitorController.java:21-27` |
| 登记访客 | ✓ | ✓ | ✗ | `VisitorController.java:36-42` |
| 登记访客离开 | ✓ | ✓ | ✗ | `VisitorController.java:44-50` |

**注意**：学生无法自行登记访客，需由宿管或管理员统一登记。

#### 3.2.6 用户管理模块

| 操作 | 管理员 | 宿管 | 学生 | 接口位置 |
|------|--------|------|------|----------|
| 新增系统用户 | ✓ | ✗ | ✗ | `UserController.java:30-35` |
| 修改用户信息 | ✓ | ✗ | ✗ | `UserController.java:37-43` |
| 删除用户 | ✓ | ✗ | ✗ | `UserController.java:45-50` |
| 启用/禁用用户 | ✓ | ✗ | ✗ | `UserController.java:52-57` |
| 分页查询用户 | ✓ | ✗ | ✗ | `UserController.java:22-28` |

---

## 四、核心模块数据流转关系

### 4.1 维修申请数据流转

```
学生端提交
    ↓（自动填充学生ID、房间ID）
维修申请表 (repair_request)
    ↓（状态：0=待处理）
管理员/宿管处理
    ↓（更新状态、添加回复）
状态流转：待处理 → 处理中 → 已完成/已拒绝
```

**维修状态定义**：
| 状态值 | 状态说明 |
|--------|----------|
| 0 | 待处理 |
| 1 | 处理中 |
| 2 | 已完成 |
| 3 | 已拒绝 |

### 4.2 访客登记数据流转

```
管理员/宿管登记
    ↓（记录访客信息、关联被访学生）
访客记录表 (visitor_record)
    ↓（状态：0=来访中）
访客离开操作
    ↓（记录离开时间、更新状态）
状态流转：来访中 → 已离开
```

### 4.3 公告发布数据流转

```
管理员/宿管创建公告
    ↓（记录发布人ID）
公告表 (announcement)
    ↓（状态：1=发布）
所有用户可见
    ↓
学生仅可查看，不可编辑
```

### 4.4 操作日志记录

系统通过 AOP 切面 `@OperationLog` 自动记录关键操作，包含：
- 操作人、操作时间
- 操作模块、操作类型
- 请求参数、IP地址
- 用于审计追溯

---

## 五、关键技术实现

### 5.1 权限校验机制（`RoleCheckAspect.java`）

通过 AOP 切面拦截带 `@RequireRole` 注解的接口，从 JWT Token 中解析用户角色，校验当前用户角色是否在允许列表中。

### 5.2 事务管理

核心业务操作（分配床位、退宿、删除学生）均使用 `@Transactional` 注解保证数据一致性：
- 床位状态更新 + 房间人数更新 在同一事务中
- 删除学生时自动解除床位绑定、删除关联用户账号

### 5.3 数据完整性约束

- 学号唯一约束
- 同一楼栋内房间号唯一
- 删除房间时校验：有学生入住的房间不可删除

---

## 六、业务闭环总结

### 6.1 完整业务链路

```
【基础配置】
管理员创建楼栋 → 创建房间（自动生成床位）

【入住流程】
新增学生 → 分配床位 → 学生入住

【日常运营】
学生提交维修申请 → 宿管处理
访客登记 → 访客离开登记
发布公告 → 学生查看

【退宿流程】
学生退宿申请 → 宿管确认 → 释放床位 → 床位回到空闲池
```

### 6.2 数据隔离原则

1. **管理员**：全量数据可见，可配置系统基础数据
2. **宿管**：负责日常运营，不可修改系统级配置
3. **学生**：仅可见个人数据，不可操作管理功能

---

**文档生成时间**：2026-04-16  
**基于代码版本**：当前仓库最新版本
