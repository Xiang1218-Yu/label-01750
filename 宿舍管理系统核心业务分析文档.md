# 宿舍管理系统核心业务分析文档

## 一、系统概述

本宿舍管理系统是基于 Spring Boot 3 + Vue 3 + MySQL 8 构建的全栈管理系统，采用基于角色的访问控制（RBAC）模型，实现了管理员、宿管、学生三个角色的权限隔离与业务协同。

---

## 二、学生入住-退宿完整业务闭环流程

### 2.1 业务流程图

```
学生信息录入 → 床位分配 → 入住生效 →（日常管理）→ 退宿申请 → 床位释放
     ↓              ↓                                  ↑
  创建账号      更新床位状态                          回收床位
```

### 2.2 详细业务流程

#### 阶段一：学生信息录入与账号创建

**触发角色**：管理员 / 宿管

**业务流程**：
1. 管理员/宿管在学生管理模块录入学生基本信息（学号、姓名、学院、专业、班级、性别、手机号、入学日期）
2. 系统自动验证学号唯一性
3. 系统自动创建系统用户账号：
   - 用户名 = 学号
   - 默认密码 = 123456（BCrypt加密存储）
   - 角色 = 学生（role=3）
   - 状态 = 启用
4. 建立学生信息与系统用户的关联（student.user_id）

**关键代码位置**：`StudentService.java:35-51`

**数据流转**：
```
输入：学生基本信息
↓
student表：插入学生记录，获取student.id
sys_user表：插入用户记录，获取user.id
↓
更新student.user_id = sys_user.id
```

---

#### 阶段二：床位分配（入住）

**触发角色**：管理员 / 宿管

**前置条件**：
1. 学生已录入系统且未分配床位
2. 目标床位状态为空闲（status=0）

**业务流程**：
1. 管理员/宿管在学生管理模块选择学生
2. 选择空闲床位（需先选择楼栋→房间→床位）
3. 系统验证：
   - 学生是否已有床位
   - 床位是否存在且空闲
4. 执行床位绑定：
   - 更新bed表：student_id=学生ID, status=1（已占用）
   - 更新room表：current_count = current_count + 1

**关键代码位置**：`StudentService.java:87-111`

**数据流转**：
```
输入：studentId, bedId
↓
验证：student表（存在）、bed表（无studentId且status=0）
↓
bed表：更新student_id、status=1
room表：current_count++
```

---

#### 阶段三：日常住宿管理

学生入住后可进行的操作：
1. **查看个人宿舍信息**：学生可查看自己的床位、房间、楼栋信息
2. **提交维修申请**：学生可针对所在宿舍提交维修申请
3. **查看公告**：学生可查看系统发布的公告

---

#### 阶段四：退宿与床位释放

**触发角色**：管理员 / 宿管

**前置条件**：学生已分配床位

**业务流程**：
1. 管理员/宿管在学生管理模块选择需要退宿的学生
2. 系统查找学生当前床位信息
3. 执行床位释放：
   - 更新bed表：student_id=null, status=0（空闲）
   - 更新room表：current_count = max(0, current_count - 1)
4. 学生床位信息清空

**关键代码位置**：`StudentService.java:113-128`

**数据流转**：
```
输入：studentId
↓
查找：bed表中student_id对应的床位
↓
bed表：清空student_id、status=0
room表：current_count--
```

---

#### 阶段五：学生信息删除（可选）

**触发角色**：管理员 / 宿管

**业务流程**：
1. 删除学生信息前，系统自动释放该学生的床位（如有）
2. 删除关联的系统用户账号
3. 逻辑删除学生记录（deleted=1）

**关键代码位置**：`StudentService.java:73-85`

---

## 三、房间与床位管理流程

### 3.1 房间创建

**触发角色**：管理员

**业务流程**：
1. 管理员选择楼栋，录入房间号、容量
2. 系统验证同一楼栋内房间号唯一性
3. 创建房间记录（current_count=0, status=1启用）
4. **自动创建床位**：根据房间容量自动生成对应数量的床位（bed_number=1~capacity, status=0空闲）

**关键代码位置**：`RoomService.java:41-58`

**数据流转**：
```
输入：buildingId, roomNumber, capacity
↓
验证：同一buildingId下roomNumber不重复
↓
room表：插入记录
bed表：循环插入capacity条床位记录
```

### 3.2 房间删除

**触发角色**：管理员

**前置条件**：房间内所有床位均为空闲

**业务流程**：
1. 检查房间内是否有已占用床位（status=1）
2. 如有占用床位，禁止删除并提示
3. 如无占用，先删除该房间下所有床位，再删除房间记录

**关键代码位置**：`RoomService.java:79-88`

---

## 四、角色权限隔离体系

### 4.1 角色定义

| 角色编码 | 角色名称 | 说明 |
|---------|---------|------|
| 1 | 管理员 | 系统最高权限，可管理所有模块 |
| 2 | 宿管 | 楼栋管理人员，可管理日常业务 |
| 3 | 学生 | 普通用户，仅可操作个人相关功能 |

### 4.2 权限控制实现机制

系统采用注解式接口级别权限控制：
- 注解：`@RequireRole`
- 切面：`RoleCheckAspect.java`
- 权限验证在请求进入Controller前执行

---

### 4.3 各角色权限矩阵

#### 🔴 模块一：楼栋管理

| 操作 | 管理员 | 宿管 | 学生 |
|------|-------|------|------|
| 查看楼栋列表（分页） | ✅ | ❌ | ❌ |
| 获取楼栋下拉列表 | ✅ | ✅ | ❌ |
| 新增楼栋 | ✅ | ❌ | ❌ |
| 修改楼栋 | ✅ | ❌ | ❌ |
| 删除楼栋 | ✅ | ❌ | ❌ |
| 修改楼栋状态 | ✅ | ❌ | ❌ |

**关键代码**：`BuildingController.java:18-64`

> **说明**：楼栋管理是系统基础配置，仅管理员可操作。宿管仅可在下拉选择时查看楼栋列表。

---

#### 🔴 模块二：房间管理

| 操作 | 管理员 | 宿管 | 学生 |
|------|-------|------|------|
| 查看房间列表（分页） | ✅ | ✅ | ❌ |
| 查看房间详情 | ✅ | ✅ | ❌ |
| 新增房间（含自动创建床位） | ✅ | ✅ | ❌ |
| 修改房间信息 | ✅ | ✅ | ❌ |
| 删除房间 | ✅ | ✅ | ❌ |
| 查看房间床位列表 | ✅ | ✅ | ❌ |
| 修改房间状态 | ✅ | ✅ | ❌ |

**关键代码**：`RoomController.java:17-67`

---

#### 🔴 模块三：学生管理

| 操作 | 管理员 | 宿管 | 学生 |
|------|-------|------|------|
| 查看学生列表（分页） | ✅ | ✅ | ❌ |
| 新增学生（含自动创建账号） | ✅ | ✅ | ❌ |
| 修改学生信息 | ✅ | ✅ | ❌ |
| 删除学生（含释放床位） | ✅ | ✅ | ❌ |
| 分配床位（入住） | ✅ | ✅ | ❌ |
| 解除床位（退宿） | ✅ | ✅ | ❌ |

**关键代码**：`StudentController.java:17-64`

---

#### 🔴 模块四：维修管理

| 操作 | 管理员 | 宿管 | 学生 |
|------|-------|------|------|
| 查看所有维修申请 | ✅ | ✅ | ❌ |
| 查看我的维修申请 | ✅ | ✅ | ✅ |
| 提交维修申请 | ✅ | ✅ | ✅ |
| 处理维修申请（审核、回复） | ✅ | ✅ | ❌ |

**关键代码**：`RepairController.java:17-52`

**特殊说明**：
- 学生提交维修申请时，系统自动获取当前登录学生信息及其床位所在房间
- 管理员/宿管可代为提交维修申请（需指定studentId和roomId）

---

#### 🔴 模块五：访客管理

| 操作 | 管理员 | 宿管 | 学生 |
|------|-------|------|------|
| 查看所有访客记录 | ✅ | ✅ | ❌ |
| 登记访客 | ✅ | ✅ | ❌ |
| 登记访客离开 | ✅ | ✅ | ❌ |

**关键代码**：`VisitorController.java:17-50`

> **说明**：访客登记由管理员或宿管操作，学生无权限自行登记访客。

---

#### 🔴 模块六：用户管理与系统管理

| 操作 | 管理员 | 宿管 | 学生 |
|------|-------|------|------|
| 用户管理（增删改查） | ✅ | ❌ | ❌ |
| 操作日志查看 | ✅ | ❌ | ❌ |
| 公告管理（发布、编辑） | ✅ | ✅ | ❌ |
| 公告查看 | ✅ | ✅ | ✅ |

---

### 4.4 权限控制总结

```
┌─────────────────────────────────────────────────────────┐
│                    权限层级关系                          │
├─────────────────────────────────────────────────────────┤
│  管理员（ADMIN=1）                                       │
│  ├── 楼栋管理（全权）                                    │
│  ├── 房间管理（全权）                                    │
│  ├── 学生管理（全权）                                    │
│  ├── 维修管理（查看+处理）                               │
│  ├── 访客管理（全权）                                    │
│  ├── 用户管理（全权）                                    │
│  └── 系统日志（查看）                                    │
│                                                         │
│  宿管（DORM_MANAGER=2）                                  │
│  ├── 楼栋管理（仅查看下拉列表）                           │
│  ├── 房间管理（全权）                                    │
│  ├── 学生管理（全权）                                    │
│  ├── 维修管理（查看+处理）                               │
│  ├── 访客管理（全权）                                    │
│  └── 公告管理（发布）                                    │
│                                                         │
│  学生（STUDENT=3）                                       │
│  ├── 查看个人宿舍信息                                    │
│  ├── 提交+查看个人维修申请                               │
│  └── 查看公告                                            │
└─────────────────────────────────────────────────────────┘
```

---

## 五、核心模块数据流转关系

### 5.1 楼栋-房间-床位-学生 数据链路

```
building（楼栋）
    │
    ├── 1:N → room（房间）
    │       └── 1:N → bed（床位）
    │                   └── 1:1 → student（学生）
    │
    └── 1:1 → sys_user（宿管）
```

**数据流转说明**：
1. **楼栋→房间**：创建房间时必须指定所属楼栋（room.building_id）
2. **房间→床位**：创建房间时自动根据容量创建床位（bed.room_id）
3. **床位→学生**：分配床位时建立关联（bed.student_id）
4. **学生→用户**：创建学生时自动创建用户账号（student.user_id）

---

### 5.2 维修申请数据流转

```
student（学生） ── 1:N ── repair_request（维修申请）
                                    │
room（房间） ─── 1:N ───────────────┘
```

**业务流程**：
1. **提交申请**：
   - 学生端：自动获取当前登录学生 → 查找床位 → 获取roomId
   - 管理端：直接指定studentId和roomId
2. **状态流转**：待处理(0) → 处理中(1) → 已完成(2)/已拒绝(3)
3. **数据关联**：维修申请记录学生和房间信息，便于定位和处理

---

### 5.3 访客登记数据流转

```
student（学生） ── 1:N ── visitor_record（访客记录）
```

**业务流程**：
1. **访客登记**：管理员/宿管录入访客信息，指定被访学生
2. **状态流转**：来访中(0) → 已离开(1)
3. **时间记录**：自动记录来访时间，离开时手动登记离开时间

---

### 5.4 操作日志数据流转

系统通过AOP切面（`OperationLogAspect.java`）自动记录关键操作：

```
Controller方法
    ↓
@OperationLog注解
    ↓
AOP切面拦截
    ├── 获取当前用户
    ├── 获取模块名、操作名
    ├── 获取方法参数
    ├── 获取IP地址
    ↓
sys_operation_log表
```

**记录的操作包括**：
- 学生管理：新增、修改、删除、分配床位、退宿
- 楼栋管理：新增、修改、删除、修改状态
- 房间管理：新增、修改、删除、修改状态
- 维修管理：提交申请、处理申请
- 访客管理：登记访客、访客离开

---

## 六、关键业务约束与数据一致性

### 6.1 床位分配约束

1. **唯一性约束**：一个学生只能有一个床位（`StudentService.bindBed` 验证）
2. **互斥性约束**：一个床位只能分配给一个学生（bed.status=1时不可重复分配）
3. **计数一致性**：房间current_count = 该房间下status=1的床位数量

### 6.2 删除保护约束

1. **房间删除保护**：房间内有学生入住时不可删除（`RoomService.delete` 验证）
2. **学生删除级联**：删除学生时自动释放床位、删除关联用户

### 6.3 数据完整性保障

1. **事务管理**：核心业务操作使用 `@Transactional` 保证数据一致性
2. **逻辑删除**：重要数据采用逻辑删除（deleted字段），避免物理删除
3. **重复校验**：学号、房间号（同楼栋内）等业务字段做唯一性校验

---

## 七、系统架构总结

### 7.1 技术架构

```
┌─────────────────────────────────────────────────────────┐
│  前端（Vue 3 + Element Plus + Pinia）                    │
│  ├── 路由权限控制                                         │
│  ├── 状态管理                                             │
│  └── API调用层                                            │
├─────────────────────────────────────────────────────────┤
│  后端（Spring Boot 3 + MyBatis-Plus）                    │
│  ├── Controller层：接口定义 + 权限注解                    │
│  ├── Service层：业务逻辑 + 事务管理                       │
│  ├── Mapper层：数据访问                                   │
│  ├── AOP切面：权限校验 + 操作日志                         │
│  └── 拦截器：JWT认证                                      │
├─────────────────────────────────────────────────────────┤
│  数据库（MySQL 8）                                        │
│  └── 13张业务表 + 索引优化                                │
└─────────────────────────────────────────────────────────┘
```

### 7.2 核心设计亮点

1. **基于注解的RBAC权限控制**：`@RequireRole` 注解实现接口级别的细粒度权限控制
2. **AOP操作日志**：自动记录关键操作，支持审计追溯
3. **事务一致性**：核心业务使用事务，保证数据操作原子性
4. **分层设计**：Controller-Service-Mapper 三层架构，职责清晰

---

## 八、附录：核心代码文件索引

| 功能模块 | 文件路径 | 关键行 |
|---------|---------|--------|
| 学生入住/退宿 | `backend/src/main/java/com/dorm/service/StudentService.java` | 87-128 |
| 房间创建（含床位） | `backend/src/main/java/com/dorm/service/RoomService.java` | 41-58 |
| 维修申请 | `backend/src/main/java/com/dorm/service/RepairService.java` | 39-70 |
| 访客登记 | `backend/src/main/java/com/dorm/service/VisitorService.java` | 36-65 |
| 角色权限注解 | `backend/src/main/java/com/dorm/annotation/RequireRole.java` | - |
| 权限校验切面 | `backend/src/main/java/com/dorm/aspect/RoleCheckAspect.java` | - |
| 操作日志切面 | `backend/src/main/java/com/dorm/aspect/OperationLogAspect.java` | - |
| 数据库脚本 | `backend/src/main/resources/schema.sql` | - |
